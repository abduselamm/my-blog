---
title: How to install react
date: '2022-09-28'
tags:
  ['react', 'install react', 'how to install react on ubuntu', 'how to install react on windows']
draft: false
summary: 'React is a free and open-source front-end JavaScript library for creating UI components-based user interfaces.'
image: 'react.png'
---

import { ImageComponent } from '@/components/ImageComponent'
import { Note } from '@/components/Note'
import { GoogleHorizontalAdUnit } from '@/components/ads'

<ImageComponent
  src="/static/images/react.png"
  width="100%"
  height="100%"
  alt="React installation illustration"
/>

## Introduction

<TOCInline toc={props.toc} exclude="Introduction" toHeading={2} />

## Introduction

React is a free and open-source front-end JavaScript library for creating UI components-based user interfaces.

- Today we are going to see how to install react both on linux and windows operating systems. Installing react is simple and the same for both operating systems.

The difference lies in the installation of Nodejs.

#### Without further ado, let get started.

## How to install react on linux

- Follow the instructions below to install react on **Linux**

#### 1. Check if Nodejs is installed

```bash
node -v
```

- Output:

```bash
v18.8.0 or similar depending on your node version.
```

- If the output is not the same as the above, then you should install Nodejs to do so follow [this article](how-to-install-nodejs)

#### 2. Installing react on linux

- Now days there are several different ways to install react but the most popular one is to use **create-react-app** package developed by the facebook itself.

- Open your terminal ( Ctrl + alt + t )

- Then run the following command to install react:

```bash
npx create-react-app my-app
```

- Where **my-app** is the name of the folder that contains the react application, you can change the name to you own preferences.

## How to install react on windows

#### 1. Check if Nodejs is installed

```bash
node -v
```

- Output:

```bash
v18.8.0 or similar depending on your node version.
```

- If the output is not the same as the above, then you should install Nodejs to do so go to [Nodejs official website](https://nodejs.org/en/download/) and download the excutable nodejs file and install the Nodejs package.

<GoogleHorizontalAdUnit path="how to install the fb react" />

#### 2. Installing react on window

- Open your terminal

- Then run the following command to install react:

```bash
npx create-react-app my-app
```

## Conclusion

Thank you for sticking with me this long to learn how to install react, until we meet in an other article by by 🚀

<GoogleHorizontalAdUnit path="how to install react" />
