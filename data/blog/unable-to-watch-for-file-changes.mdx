---
title: Visual Studio Code Unable to watch for file changes
date: '2022-09-26'
tags:
  [
    'vscode',
    'vscode unable to watch',
    'vscode unable to watch for file changes',
    'how to fix javascript heap out of memory',
    'how to fix vscode unable to watch for file changes',
  ]
draft: false
summary: 'To fix this issue you only need to increase the limit such that the limit for watching file changes for vscode.'
image: 'vscode.png'
---

import { ImageComponent } from '@/components/ImageComponent'
import { Note } from '@/components/Note'
import { GoogleHorizontalAdUnit } from '@/components/ads'

<ImageComponent
  src="/static/images/vscode.png"
  width="100%"
  height="100%"
  alt="vscode illustration"
/>

## Overview

<TOCInline toc={props.toc} exclude="Overview" toHeading={2} />

## Introduction

The visual studio code unable to watch for file changes issue occurs when vscode is unable to watch the file beyond its watch limit; to resolve this issue, simply increase the limit slightly.

<GoogleHorizontalAdUnit path="vscode unable to watch file changes" />

## Solution

#### 1. Check the current limit

```bash
cat /proc/sys/fs/inotify/max_user_watches
```

#### 2. Open sysctl.conf file

```bash
sudo nano /etc/sysctl.conf
```

#### 3. Add this line at bottom

```bash
fs.inotify.max_user_watches=524288
```

#### 4. Save and exit

- Ctrl + x then Ctrl + c

#### 5. Reload the change

```bash
sudo sysctl -p
```

#### 6. Check the limit again

```bash
cat /proc/sys/fs/inotify/max_user_watches
```

- If the count is same, execute the following command:

```bash
echo fs.inotify.max_user_watches=524288 | sudo tee -a /etc/sysctl.conf && sudo sysctl -p
```

🎉 Happy coding.

## Conclusion

Thank you for sticking with me this long, I hope this helps and I will see you later on an other article, by by 🚀.

<GoogleHorizontalAdUnit path="vscode unable to watch file changes" />
